# Rugira Trading Dashboard Environment Variables

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (Production)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here

# CORS Configuration (automatically configured based on NODE_ENV)
# Development: localhost domains allowed
# Production: app.rugira.ch and rugira.ch domains allowed

# OAuth Configuration (Update these URLs in your OAuth provider)
# Production OAuth Callbacks:
# - https://app.rugira.ch/auth/callback
# - https://rugira.ch/auth/logout (post-logout redirect)
# - https://app.rugira.ch/auth/refresh (token refresh)

# Development OAuth Callbacks:
# - http://localhost:5000/auth/callback
# - http://localhost:5000/auth/logout
# - https://your-replit-preview.replit.dev/auth/callback

# Optional: OAuth Provider Configuration
# OAUTH_CLIENT_ID=your-oauth-client-id
# OAUTH_CLIENT_SECRET=your-oauth-client-secret
# OAUTH_ISSUER=https://your-oauth-provider.com